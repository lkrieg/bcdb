#include "util.h"

TEST_SETUP(PARAMS)
{

}

TEST_TEAR_DOWN(PARAMS)
{

}

TEST(PARAMS, Parses_config_file_settings)
{
	TEST_ASSERT(1);
}

TEST(PARAMS, Parses_command_line_options)
{
	TEST_ASSERT(1);
}
